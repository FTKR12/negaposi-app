# 設計書
## フロント
概要：

## バックエンド
概要：フロントと感情分析APIの間に位置し、DBキャッシュしつつ、結果を返す。
1. 画面で入力されたテキストを受け取る。
2. 受け取ったテキストがDB（`sentiments.db`）に存在するかチェック。
    - 存在するならDBからそのテキストに対する`label`を取得。
    - 存在しないなら、感情分析APIを呼び出し、`label`を取得し、DBに`insert`する。
4. 取得した`label`を画面に返す。
